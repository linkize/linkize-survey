[build]
  command = "npm ci && npm run build"
  publish = "dist"
  functions = ".netlify/functions"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "VITE_SUPABASE_ANON_KEY,VITE_SUPABASE_URL,BREVO_API_KEY,MAIL_FROM_NAME,MAIL_FROM"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
